doctype html
html(lang="ja")
  head
    meta(charset="UTF-8")
    title アンケート
  body
    h1 どちらが食べたいですか？
    form(method="post" action=path)
      span 名前: 
      input(type="text" name="name")
      input(type="radio" name="favorite" value=firstItem checked)#firstItem
      label(for="firstItem")  #{firstItem}
      input(type="radio" name="favorite" value=secondItem)#secondItem
      label(for="secondtem")  #{secondItem}
      input(type="radio" name="favorite" value=thirdItem)#thirdItem
      label(for="thirdItem")  #{thirdItem} 
      button(type="submit") 投稿
